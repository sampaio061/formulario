<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Cadastro de Pessoa</title>
    <style>
        /* Configurações gerais da página */
        body {
            background-color: #1e1e1e;
            font-family: Consolas, "Courier New", monospace;
            color: #d4d4d4;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            margin: 0;
        }

        /* Estilo do formulário */
        form {
            background-color: #2d2d30;
            padding: 20px;
            width: 400px;
            border-radius: 8px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.5);
        }

        h1 {
            color: #569cd6;
            text-align: center;
            font-size: 1.5em;
            margin-bottom: 20px;
        }

        p {
            margin: 10px 0 5px;
            color: #9cdcfe;
        }

        input[type="text"],
        input[type="date"],
        input[type="password"],
        input[type="color"],
        input[type="email"],
        input[type="file"],
        select {
            width: 100%;
            padding: 8px;
            margin-bottom: 15px;
            background-color: #1e1e1e;
            border: 1px solid #3c3c3c;
            color: #d4d4d4;
            border-radius: 4px;
            box-sizing: border-box;
        }

        input[type="radio"],
        input[type="checkbox"] {
            margin-right: 8px;
        }

        input[type="submit"] {
            width: 100%;
            padding: 10px;
            background-color: #007acc;
            border: none;
            color: #ffffff;
            font-weight: bold;
            cursor: pointer;
            border-radius: 4px;
            font-size: 1em;
            margin-top: 15px;
        }

        input[type="submit"]:hover {
            background-color: #005a9e;
        }
    </style>
</head>
<body>
    <form action="recebeDados.java" method="get">
        <h1>Cadastro de Pessoa</h1>
        
        <p>Nome:</p>
        <input type="text" placeholder="Digite seu nome" name="nome">
        
        <p>Data de Nascimento:</p>
        <input type="date" name="dataNascimento">
        
        <p>Senha:</p>
        <input type="password" name="senha">
        
        <p>Cor:</p>
        <input type="color" name="cor">
        
        <p>Email:</p>
        <input type="email" name="email">
        
        <p>Mande a foto:</p>
        <input type="file" name="arquivo">
        
        <p>Escolaridade:</p>
        <input type="radio" name="escolaridade" value="Ensino médio"> Ensino médio<br>
        <input type="radio" name="escolaridade" value="Superior"> Superior<br>
        <input type="radio" name="escolaridade" value="Fundamental"> Fundamental<br>
        
        <p>Disciplinas:</p>
        <input type="checkbox" name="lista[]" value="java"> Java<br>
        <input type="checkbox" name="lista[]" value="php"> PHP<br>
        <input type="checkbox" name="lista[]" value="sql"> SQL<br>
        
        <p>Estado:</p>
        <select name="estado">
            <option value="escolha" selected>Escolha</option>
            <option value="mg">MG</option>
            <option value="df">DF</option>
            <option value="go">GO</option>
        </select>
        
        <input type="submit" value="Cadastrar">
    </form>
</body>
</html>       depois de ve todas as informações que te dei até agora, faça Relatório Final

Descrição do projeto, análise estática, plano de refatoração e resultados 
